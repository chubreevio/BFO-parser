# BFO Parser

API сервис для получения бухгалтерской финансовой отчётности (БФО) российских организаций с портала ФНС России.

## Описание

BFO Parser - это FastAPI приложение, которое предоставляет API для получения финансовых отчётов организаций по ИНН. Сервис интегрируется с порталом https://bo.nalog.gov.ru, кэширует данные в базе данных и использует Redis для ограничения частоты запросов.

### Основные возможности

- 🔍 Поиск организаций по ИНН
- 📊 Получение бухгалтерской финансовой отчётности за выбранные периоды
- 💾 Кэширование данных в PostgreSQL (отчёты актуальны 7 дней)
- 🚦 Защита от превышения лимитов запросов к ФНС через Redis
- 🔄 Две версии API с разной логикой кэширования
- 📝 Автоматическое логирование запросов
- 🐳 Полная контейнеризация с Docker Compose

## Технологический стек

- **Backend**: FastAPI 0.124.4
- **Database**: PostgreSQL 15.3
- **Cache/Queue**: Redis 7.2.10
- **ORM**: SQLAlchemy 2.0.45
- **Migrations**: Alembic 1.17.2
- **HTTP Client**: aiohttp 3.13.2
- **Logger**: Loguru 0.7.3
- **Server**: Uvicorn 0.38.0

## Архитектура

```
app/
├── api/                    # API слой
│   ├── endpoints/          # Обработчики эндпоинтов
│   ├── exception_handlers/ # Обработчики исключений
│   ├── middlewares/        # Промежуточное ПО
│   └── routers.py          # Роутинг
├── db/                     # Слой базы данных
|   ├── _migrations/        # Миграции alembic
│   ├── organization/       # Репозиторий организаций
│   ├── report/             # Репозиторий отчётов
│   ├── history/            # История запросов
│   └── sqlalchemy.py       # Конфигурация БД
├── helpers/                # Вспомогательные функции
├── schemas/                # Pydantic схемы
├── exceptions.py           # Кастомные исключения
├── logger.py               # Конфигурация логирования
├── settings.py             # Настройки приложения
└── startup.py              # Инициализация приложения
```

## Установка и запуск

### Быстрый старт

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd BFO-parser
```

2. Создайте файл `.env` на основе `example.env`:
```bash
cp example.env .env
```

3. Отредактируйте `.env` файл, указав необходимые параметры:
```env
DEBUG=true
BFO_URL=https://bo.nalog.gov.ru
PROXY_URL=https://user:password@host:port  # Опционально

# Redis
REDIS_HOST=bfo-parser-api--redis
REDIS_PORT=6379

# PostgreSQL
DB_HOSTNAME=bfo-parser-api--db
DB_DATABASE=bfo-parser
DB_USERNAME=bfo-parser-user
DB_PASSWORD=strongpassword
DB_PORT=5432
DB_EXTERNAL_PORT=25432
POSTGRES_DSN=postgres://${DB_USERNAME}:${DB_PASSWORD}@${DB_HOSTNAME}:${DB_PORT}/${DB_DATABASE}

# Настройки
SQL_DEBUG=false
REPORT_AVAILABLE_DAYS=7

# Тесты
DB_TEST_HOSTNAME=bfo-parser-api--db-test
DB_TEST_EXTERNAL_PORT=25433
DB_TEST_PASSWORD=teststrongpassword
TEST_POSTGRES_DSN=postgres://${DB_USERNAME}:${DB_TEST_PASSWORD}@${DB_TEST_HOSTNAME}:${DB_PORT}/${DB_DATABASE}
```

4. Запустите приложение:
```bash
docker-compose up -d
```

5. Проверьте статус контейнеров:
```bash
docker-compose ps
```

Приложение будет доступно по адресу: http://localhost:8000

API документация (Swagger): http://localhost:8000/docs

## API Endpoints

### V1 API - Консервативное кэширование

#### GET `/api/v1/report`

Получение БФО отчёта организации. **Логика**: Если есть хотя бы один отчёт в БД (не старше 7 дней), запрос к ФНС не выполняется.

**Query параметры:**
- `inn` (required, string): ИНН организации (10 или 12 цифр)
- `periods` (optional, array): Массив годов для получения отчётов (например: `2022,2023`)

**Пример запроса:**
```bash
# Получить последний отчёт
curl "http://localhost:8000/api/v1/report?inn=7707083893"

# Получить отчёты за конкретные годы
curl "http://localhost:8000/api/v1/report?inn=7707083893&periods=2022,2023"
```

**Ответ:**
```json
{
  "inn": "7707083893",
  "periods": [
    {
      "year": 2023,
      "reports": [
        {
          "id": 1,
          "report_year": 2023,
          "created_at": "2024-01-15T10:30:00Z",
          "updated_at": "2024-01-15T10:30:00Z"
        }
      ]
    }
  ]
}
```

### V2 API - Агрессивное обновление

#### GET `/api/v2/report`

Получение БФО отчёта организации. **Логика**: Если запрошенного года нет в БД, выполняется запрос к ФНС для обновления.

**Query параметры:** (аналогичны v1)

**Пример запроса:**
```bash
curl "http://localhost:8000/api/v2/report?inn=7707083893&periods=2023"
```

## Защита от rate limit

Сервис использует Redis для защиты от превышения лимита запросов к порталу ФНС:

- При получении ошибки 429 (Too Many Requests) устанавливается таймаут на 180 секунд
- Последующие запросы в течение таймаута возвращают ошибку без обращения к ФНС
- Таймаут автоматически сбрасывается по истечении времени

## База данных

### Миграции

Миграции выполняются автоматически при старте приложения через Alembic.

Для создания новой миграции:
```bash
docker-compose exec bfo-parser-api--app bash
alembic revision --autogenerate -m "Description"
alembic upgrade head
```

### Основные таблицы

- `organizations` - Организации
- `reports` - Финансовые отчёты
- `request_history` - История запросов к API

## Разработка

### Структура Middleware

1. `ErrorHandlerMiddleware` - Глобальная обработка ошибок
2. `DbSessionMiddleware` - Управление сессиями БД
3. `EndpointLoggingMiddleware` - Логирование запросов

## Конфигурация

Все настройки управляются через переменные окружения (см. `app/settings.py`):

| Переменная | Описание | По умолчанию |
|-----------|----------|--------------|
| `DEBUG` | Режим отладки | - |
| `BFO_URL` | URL портала ФНС | - |
| `PROXY_URL` | Прокси сервер (опционально) | None |
| `REPORT_AVAILABLE_DAYS` | Срок актуальности кэша (дни) | 7 |
| `REDIS_BFO_TIMEOUT_SECONDS` | Таймаут при rate limit (сек) | 180 |
| `REDIS_HOST` | Хост Redis | - |
| `REDIS_PORT` | Порт Redis | - |
| `DB_HOSTNAME` | Хост PostgreSQL | - |
| `DB_PORT` | Порт PostgreSQL | - |
| `DB_DATABASE` | Имя базы данных | - |
| `DB_USERNAME` | Пользователь БД | - |
| `DB_PASSWORD` | Пароль БД | - |
| `TEST_POSTGRES_DSN` | Строка подключения к тестовой БД | - |
| `DB_TEST_HOSTNAME` | Хост тестовой БД | - |
| `DB_TEST_PASSWORD` | Пароль тестовой БД | - |
| `DB_TEST_EXTERNAL_PORT` | Внешний порт тестовой БД | - |
